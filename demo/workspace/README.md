# Demo Workspace

This small Node.js project is included with SimpleRCPv2. It has no external dependencies.

Run the example:

```bash
npm start
```

Run its tests:

```bash
npm test
```

Open `src/projectStatus.js` in two browser windows to try collaborative editing. Use the shared terminal to run the commands again after making changes.
